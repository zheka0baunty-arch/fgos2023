# Лабораторное занятие №10. Работа с логическими томами LVM

## Объем
2 часа

## Цель
Освоить создание и управление логическими томами с использованием LVM (Logical Volume Manager). Научиться создавать физические тома, группами томов и логические тома, а также изменять их размеры и управлять ими.

## Задачи
1. Изучить архитектуру LVM и ее компоненты
2. Создать физические тома (PV) из доступных дисков
3. Создать группу томов (VG) и логические тома (LV)
4. Настроить файловые системы на логических томах
5. Освоить изменение размеров логических томов
6. Настроить постоянное монтирование LVM томов

## Методические указания

### Теоретическая часть

#### Что такое LVM?
**LVM (Logical Volume Manager)** - система управления логическими томами в Linux, позволяющая гибко управлять дисковым пространством.

#### Архитектура LVM

| Компонент | Описание | Пример |
|-----------|----------|--------|
| **Физический том (PV)** | Физическое устройство или раздел | `/dev/vdb`, `/dev/vdc` |
| **Группа томов (VG)** | Объединение нескольких PV | `vg_data` |
| **Логический том (LV)** | Логическое устройство в VG | `lv_home`, `lv_var` |

#### Преимущества LVM
- **Гибкость** - можно изменять размеры томов "на лету"
- **Объединение дисков** - несколько физических дисков в одну группу
- **Снимки** - создание моментальных снимков для резервного копирования
- **Миграция** - перенос данных между физическими дисками

#### Инструменты LVM
- **pvcreate/pvdisplay/pvremove** - работа с физическими томами
- **vgcreate/vgdisplay/vgremove** - работа с группами томов
- **lvcreate/lvdisplay/lvremove** - работа с логическими томами

### Практическая часть

#### Шаг 1: Подготовка системы
Данная работа выполняется на ВМ server.

**Проверка доступных дисков:**
```bash
# Просмотр всех блочных устройств
lsblk

# Проверка существующих LVM томов
  pvdisplay
  vgdisplay
  lvdisplay
```

#### Шаг 2: Создание физических томов

**Подготовка дисков:**
```bash
# Используем диски /dev/vdb, /dev/vdc, /dev/vdd
# Проверка, что диски не используются
  umount /dev/vdb* 2>/dev/null || true
  umount /dev/vdc* 2>/dev/null || true
  umount /dev/vdd* 2>/dev/null || true

# Очистка дисков (если нужно)
  mdadm --zero-superblock /dev/vdb /dev/vdc /dev/vdd 2>/dev/null || true
  wipefs -a /dev/vdb /dev/vdc /dev/vdd 2>/dev/null || true
```

**Создание физических томов:**
```bash
# Создание физических томов
  pvcreate /dev/vdb /dev/vdc /dev/vdd

# Проверка создания PV
  pvdisplay
  pvs
```

#### Шаг 3: Создание группы томов

**Создание VG:**
```bash
# Создание группы томов
  vgcreate vg_data /dev/vdb /dev/vdc /dev/vdd

# Проверка создания VG
  vgdisplay vg_data
  vgs
```

#### Шаг 4: Создание логических томов

**Создание LV:**
```bash
# Создание логического тома для home 
  lvcreate -L 400M -n lv_home vg_data

# Создание логического тома для var 
  lvcreate -L 300M -n lv_var vg_data

# Создание логического тома для tmp 
  lvcreate -l 500M -n lv_tmp vg_data

# Проверка создания LV
  lvdisplay
  lvs
```

#### Шаг 5: Форматирование и монтирование

**Форматирование LV:**
```bash
# Форматирование в ext4
  mkfs.ext4 /dev/vg_data/lv_home
  mkfs.ext4 /dev/vg_data/lv_var
  mkfs.ext4 /dev/vg_data/lv_tmp

# Добавление меток
  e2label /dev/vg_data/lv_home LVM_HOME
  e2label /dev/vg_data/lv_var LVM_VAR
  e2label /dev/vg_data/lv_tmp LVM_TMP
```

**Создание точек монтирования:**
```bash
# Создание каталогов
  mkdir -p /mnt/lvm_home
  mkdir -p /mnt/lvm_var
  mkdir -p /mnt/lvm_tmp

# Установка прав доступа
  chmod 755 /mnt/lvm_home
  chmod 755 /mnt/lvm_var
  chmod 755 /mnt/lvm_tmp
```

**Монтирование LV:**
```bash
# Монтирование логических томов
  mount /dev/vg_data/lv_home /mnt/lvm_home
  mount /dev/vg_data/lv_var /mnt/lvm_var
  mount /dev/vg_data/lv_tmp /mnt/lvm_tmp

# Проверка монтирования
df -h
mount | grep lvm
```

#### Шаг 6: Изменение размеров LVM томов

**Увеличение размера LV:**
```bash
# Увеличение lv_home на 100M
  lvextend -L +100M /dev/vg_data/lv_home

# Расширение файловой системы
  resize2fs /dev/vg_data/lv_home

# Проверка изменений
df -h
  lvs
```

**Уменьшение размера LV:**
```bash
# Размонтирование тома
  umount /mnt/lvm_tmp

# Проверка файловой системы
  e2fsck -f /dev/vg_data/lv_tmp

# Уменьшение файловой системы
  resize2fs /dev/vg_data/lv_tmp 200M

# Уменьшение логического тома
  lvreduce -L 200M /dev/vg_data/lv_tmp

# Повторное монтирование
  mount /dev/vg_data/lv_tmp /mnt/lvm_tmp
```

#### Шаг 7: Постоянное монтирование LVM томов

**Резервное копирование fstab:**
```bash
# Резервное копирование
  cp /etc/fstab /etc/fstab.backup
```

**Добавление записей в fstab:**
```bash
# Получение UUID томов
  blkid /dev/vg_data/lv_home
  blkid /dev/vg_data/lv_var
  blkid /dev/vg_data/lv_tmp

# Добавление записей в fstab
echo 'UUID=ваш-uuid-lv_home /mnt/lvm_home ext4 defaults 0 2' |   tee -a /etc/fstab
echo 'UUID=ваш-uuid-lv_var /mnt/lvm_var ext4 defaults 0 2' |   tee -a /etc/fstab
echo 'UUID=ваш-uuid-lv_tmp /mnt/lvm_tmp ext4 defaults 0 2' |   tee -a /etc/fstab
```

**Проверка конфигурации:**
```bash
# Проверка синтаксиса
  mount -a

# Проверка успешного монтирования
df -h
mount | grep lvm
```

## Требования к отчету

**ВАЖНО:** В данной лабораторной работе настроенная виртуальная машина является основным отчетом. Дополнительные письменные отчеты не требуются.

### Проверочный чек-лист для преподавателя

**Базовые требования (обязательные):**
- [ ] Созданы физические тома (PV)
- [ ] Создана группа томов (VG)
- [ ] Созданы логические тома (LV)
- [ ] LV отформатированы в файловые системы
- [ ] Настроено постоянное монтирование

**Дополнительные требования:**
- [ ] Продемонстрировано изменение размеров LV
- [ ] Создана иерархия каталогов в смонтированных томах
- [ ] Проверена работоспособность LVM системы
- [ ] Настроена система мониторинга

## Контрольные вопросы

1. **Что такое LVM и для чего он используется?**
  
2. **Какие компоненты входят в архитектуру LVM?**
 
3. **Какие преимущества LVM перед обычными разделами?**
 
4. **Как добавить новый диск в существующую VG?**
 
5. **Как увеличить размер LV?**
 
6. **Что такое LVM снимки?**
 
7. **Как проверить состояние LVM системы?**
 
8. **Как удалить LV?**
 
9. **Что такое PE в LVM?**
 
10. **Какие файловые системы можно использовать с LVM?**
 
## Типичные проблемы и их решения

### Проблема 1: "No space left on device" при создании LV
**Причина:** Недостаточно свободного места в VG
**Решение:**
```bash
# Проверить свободное место
  vgdisplay vg_data
# Уменьшить размер других LV или добавить новый PV
```

### Проблема 2: "mount: unknown filesystem type 'LVM2_member'"
**Причина:** Не активированы LV
**Решение:**
```bash
# Активировать LV
  vgchange -ay
# Или активировать конкретную VG
  vgchange -ay vg_data
```

### Проблема 3: Ошибка при изменении размера LV
**Причина:** Файловая система не позволяет изменение размера
**Решение:**
```bash
# Проверить файловую систему
  e2fsck -f /dev/vg_data/lv_name
# Для уменьшения: сначала уменьшить ФС, потом LV
```

### Проблема 4: LV не виден после перезагрузки
**Причина:** Не добавлен в fstab или не активирован
**Решение:**
```bash
# Активировать VG
  vgchange -ay
# Проверить fstab
  cat /etc/fstab
```

### Проблема 5: Ошибка при добавлении диска в VG
**Причина:** Диск не является PV или занят
**Решение:**
```bash
# Создать PV
  pvcreate /dev/диск
# Проверить занятость диска
  lsof +D /путь
```

## Рекомендации по безопасности

1. **Резервное копирование данных**
   - Всегда создавайте резервные копии перед изменением размеров
   - Используйте LVM снимки для резервного копирования

2. **Проверка команд**
   - Внимательно проверяйте команды перед выполнением
   - Убедитесь, что указываете правильные устройства

3. **Работа с правами root**
   - Выполняйте команды с минимально необходимыми привилегиями
   - Не используйте ` ` без необходимости

4. **Мониторинг системы**
   - Регулярно проверяйте состояние LVM
   - Используйте `pvs`, `vgs`, `lvs` для мониторинга

5. **Настройка оповещений**
   - Настройте мониторинг свободного места в VG
   - Используйте cron для регулярной проверки состояния

## Дополнительные материалы для изучения

1. **Официальная документация LVM**
   - `man lvm`
   - `man lvcreate`
   - `man vgcreate`

2. **Документация по LVM**
   - https://wiki.archlinux.org/title/LVM
   - https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/6/html/logical_volume_manager_administration/

3. **Практические руководства**
   - "LVM HOWTO" - документация Linux
   - "LVM Configuration Guide"

4. **Онлайн ресурсы**
   - https://www.tecmint.com/extend-and-reduce-lvms-in-linux/
   - https://www.linuxtechi.com/create-lvm-storage-linux/

## Заключение

В ходе выполнения данной лабораторной работы вы освоили:
- Создание и управление физическими томами
- Работу с группами томов и логическими томами
- Изменение размеров LVM томов
- Настройку постоянного монтирования

Эти навыки являются фундаментальными для системного администратора и будут использоваться при решении более сложных задач, таких как управление хранилищами данных, настройка серверов и обеспечение гибкости дискового пространства.

Помните, что правильная настройка и мониторинг LVM - это основа гибкой и надежной системы хранения данных. Всегда тестируйте свои действия в безопасной среде и создавайте резервные копии перед внесением изменений.
