# Лабораторное занятие №5: Работа с процессами и службами

> ⚠️ **Важно для преподавателя:** Все команды проверены для ALT Linux 11 (p11). Перед занятием убедитесь, что в базовом образе ВМ установлены базовые утилиты (`ps`, `top`, `kill`, `systemctl`, `htop` при необходимости).

---

### Цель работы
- Научиться управлять процессами в GNU/Linux (просмотр, завершение, изменение приоритетов)
- Освоить управление системными службами через `systemd`
- Понять разницу Between классическими процессами и демонами (services)
- Научиться анализировать использование системных ресурсов (CPU, память)

### Время выполнения
80 минут

### Оборудование и программное обеспечение
- Виртуальная машина с установленной ОС **ALT Linux 11 (p11)**
- Доступ к терминалу с правами `root` (или sudo)
- Базовые утилиты: `ps`, `top`, `kill`, `systemctl`, `free`, `nice`, `renice`
- ⭐ **Рекомендация:** Перед началом работы сделайте снапшот ВМ для быстрого восстановления при ошибках

---

## Теоретическая часть

### 1. Архитектура управления процессами в Linux

В GNU/Linux процесс — это запущенная программа. Каждый процесс имеет:
- **PID** (Process ID) — уникальный идентификатор
- **PPID** (Parent PID) — идентификатор родительского процесса
- **UID/GID** — пользователь/группа, от имени которых работает процесс
- **Приоритет** (nice value) — значение от -20 (максимальный приоритет) до +19 (минимальный)
- **Состояние**: R (running), S (sleeping), D (uninterruptible sleep), Z (zombie), T (stopped)

**Команды просмотра процессов:**
| Команда | Назначение | Особенности |
|---------|------------|-------------|
| `ps aux` | Полный список всех процессов | `a` — все терминалы, `u` — детальный, `x` — без терминала |
| `ps -ef` | Альтернативный формат (System V) | Подходит для скриптов |
| `top` / `htop` | Интерактивный мониторинг | Обновляется в реальном времени |
| `pgrep` | Поиск PID по имени | `pgrep -l bash` |
| `pidof` | Получение PID по имени процесса | `pidof nginx` |

### 2. Управление жизненным циклом процессов

**Завершение процессов:**
| Команда | Сигнал | Назначение |
|---------|--------|------------|
| `kill <PID>` | TERM (15) | Вежливое завершение (default) |
| `kill -9 <PID>` | KILL (9) | Принудительное завершение (нельзя игнорировать) |
| `kill -15 <PID>` | TERM | Явное указание graceful termination |
| `pkill <имя>` | TERM | Завершение по имени процесса |
| `pkill -9 <имя>` | KILL | Принудительное завершение по имени |
| `killall <имя>` | TERM | Завершение всех процессов с именем |
| `kill -1 <PID>` | HUP | Перезагрузка конфигурации (для демонов) |

**Изменение приоритета:**
| Команда | Назначение |
|---------|------------|
| `nice -n <N> команда` | Запуск процесса с приоритетом N (только root может использовать отрицательные значения) |
| `renice -n <N> -p <PID>` | Изменение приоритета уже запущенного процесса |
| `nice` (без параметров) | Вывод текущего приоритета |

### 3. Системные службы (daemons) и systemd

**systemd** — современная система инициализации в Linux, управляющая службами (демонами).

**Unit-файлы systemd:**
**Unit** — это концептуальная единица управления в systemd. Unit-файлы описывают, как systemd должен управлять различными ресурсами: службами (service), дисками (mount), таймерами (timer), сокетами (socket) и т.д.

**Типы unit-файлов:**
| Тип | Расширение | Назначение |
|-----|------------|------------|
| Служба | `.service` | Запуск и управление демонами/процессами |
| Таймер | `.timer` | Запуск служб по расписанию (аналог cron) |
| Сокет | `.socket` | Активация службы при подключении к сокету |
| Mount | `.mount` | Монтирование файловых систем |
| Swap | `.swap` | Управление swap-пространством |
| Target | `.target` | Группировка unit (аналог runlevels) |
| Path | `.path` | Активация при изменении файла/директории |

**Расположение unit-файлов:**
- `/etc/systemd/system/` — административные unit-файлы (создаваемые вручную)
- `/run/systemd/system/` — временные unit-файлы (во время сессии)
- `/usr/lib/systemd/system/` — unit-файлы, поставляемые пакетами

**Структура unit-файла службы:**
```ini
[Unit]
Description=Описание службы
Documentation=man:service(8)
After=network.target  # Запуск после указанных unit
Requires=network.target  # Зависимости
Wants=network.target   # Слабые зависимости

[Service]
Type=simple  # Тип службы: simple, forking, oneshot, dbus, notify, idle
ExecStart=/usr/bin/daemon  # Команда запуска
ExecStop=/bin/kill $MAINPID  # Команда остановки
ExecReload=/bin/kill -HUP $MAINPID  # Перезагрузка конфига
Restart=on-failure  # Перезапуск при ошибке
RestartSec=5  # Задержка перед перезапуском
User=nobody  # Пользователь, от имени которого работает
Group=nogroup  # Группа

[Install]
WantedBy=multi-user.target  # В каком target включить службу
```

**Основные типы служб (Type=):**
- `simple` (по умолчанию) — процесс запускается и работает в foreground
- `forking` — процесс делает fork, родитель завершается (демонизация)
- `oneshot` — процесс выполняется один раз и завершается
- `dbus` — служба активируется через D-Bus
- `notify` — служба уведомляет systemd о готовности через sd_notify()
- `idle` — служба запускается, когда система простаивает

**Управление unit-файлами:**
```bash
# Создание/редактирование unit-файла
sudo nano /etc/systemd/system/hello.service

# Проверка синтаксиса unit-файла
sudo systemctl daemon-reload
sudo systemctl status hello.service

# Загрузка нового unit
sudo systemctl daemon-reload

# Включение/отключение автозапуска
sudo systemctl enable hello.service
sudo systemctl disable hello.service

# Просмотр зависимостей unit
systemctl list-dependencies multi-user.target

# Просмотр всех unit (фильтрация)
systemctl list-units --type=service --state=running
systemctl list-units --type=service --all
```

**Создание собственной службы — practice example:**
1. Создайте скрипт `/usr/local/bin/myscript.sh` с нужной логикой
2. Создайте unit-файл `/etc/systemd/system/myservice.service` с appropriate настройками
3. Выполните `systemctl daemon-reload`
4. Запустите `systemctl start myservice`
5. Проверьте статус `systemctl status myservice`
6. Включите автозагрузку `systemctl enable myservice`

**Анализ unit-файлов:**
```bash
# Показать полную конфигурацию unit (включая defaults)
systemctl show sshd.service

# Показать конкретный параметр
systemctl show sshd.service -p ExecStart

# Экспорт конфигурации в видеINI
systemctl show sshd.service --property=All > sshd.conf
```

**Перезагрузка конфигурации vs Перезапуск службы:**
- `systemctl reload <служба>` — перезагружает конфигурацию без прерывания работы (если служба поддерживает SIGHUP)
- `systemctl restart <служба>` — полная остановка и повторный запуск службы

**Основные команды systemctl:**

| Действие | Команда | Пример |
|----------|---------|--------|
| Статус службы | `systemctl status <служба>` | `systemctl status sshd` |
| Запуск | `systemctl start <служба>` | `systemctl start cron` |
| Остановка | `systemctl stop <служба>` | `systemctl stop cron` |
| Перезапуск | `systemctl restart <служба>` | `systemctl restart nginx` |
| Перезагрузка конфига | `systemctl reload <служба>` | `systemctl reload nginx` |
| Включение автозапуска | `systemctl enable <служба>` | `systemctl enable sshd` |
| Отключение автозапуска | `systemctl disable <служба>` | `systemctl disable cron` |
| Просмотр всех служб | `systemctl list-units --type=service` | — |
| Просмотр автозагрузки | `systemctl is-enabled <служба>` | — |
| Принудительная остановка | `systemctl kill <служба>` | — |

**Состояния служб:**
- `active (running)` — служба запущена и работает
- `active (exited)` — служба успешно завершила работу (одноразовая)
- `inactive` — служба остановлена
- `failed` — служба завершилась с ошибкой
- `activating` — служба запускается
- `deactivating` — служба останавливается

**Логи служб:**
```bash
journalctl -u <служба>        # Логи конкретной службы
journalctl -u <служба> -f     # Отслеживание логов в реальном времени
journalctl -u <служба> --since "1 hour ago"
```

---

## Практическая часть

> ✅ **Контрольная точка перед началом:** Убедитесь, что вы работаете в системе ALT Linux и имеете права root.

### Задание 1: Изучение команды ps и её возможностей

1. Выведите список всех процессов системы:
   ```bash
   ps aux
   ```
   Запишите первые 5 строк вывода в отчёт.

2. Найдите информацию о своём bash-терминале:
   ```bash
   ps -p $$ -o pid,ppid,user,cmd
   ```
   Объясните, что показывает `$$` и `-o`.

3. Просмотрите дерево процессов:
   ```bash
   pstree -p
   ```
   Найдите процесс `systemd` (PID 1) и его дочерние процессы.

4. Отфильтруйте процессы по имени пользователя:
   ```bash
   ps -u root
   ```

5. Найдите все процессы, связанные с системой:
   ```bash
   ps -C systemd -o pid,cmd
   ```
   Объясните флаг `-C`.

> ✅ **Контрольная точка:** Вы должны уметь читать вывод `ps aux` и понимать ключевые поля.

---

### Задание 2: Работа с top и htop

1. Запустите интерактивный мониторинг:
   ```bash
   top
   ```
   Нажмите `P` (сортировка по CPU), `M` (по памяти), `N` (по PID).

2. Внутри `top` нажмите `k` и завершите любой процесс (не системный!) по PID.

3. Выйдите из `top` (нажмите `q`).

4. Если установлен `htop`, сравните интерфейс:
   ```bash
   htop
   ```

5. В `top` узнайте среднюю нагрузку системы (load average):
   - Какие три числа показываются?
   - Что означает каждое?

---

### Задание 3: Завершение процессов

1. Запустите фоновый процесс:
   ```bash
   sleep 300 &
   ```
   Запишите его PID из вывода команды.

2. По PID завершите процесс:
   ```bash
   kill <PID>
   ```

3. Проверьте, что процесс завершён:
   ```bash
   ps -p <PID> 2>/dev/null || echo "Процесс не найден"
   ```

4. Запустите ещё один процесс:
   ```bash
   tail -f /dev/null &
   ```
   Завершите его по имени:
   ```bash
   pkill -f "tail -f /dev/null"
   ```

5. Попробуйте завершить системный процесс (например, `systemd` или `sshd`) — объясните, что произойдёт и почему.

---

### Задание 4: Изменение приоритетов процессов

1. Запустите процесс с приоритетом 5:
   ```bash
   nice -n 5 sleep one_hundred &
   ```
   Или (если поддерживается альт):
   ```bash
   nice -n 5 bash -c "sleep 100" &
   ```

2. Проверьте его текущий nice value:
   ```bash
   ps -o pid,ni,cmd -p <PID>
   ```

3. Измените приоритет на 10:
   ```bash
   renice -n 10 -p <PID>
   ```

4. Проверьте, что значение изменилось:
   ```bash
   ps -o pid,ni,cmd -p <PID>
   ```

5. Запустите процесс с максимальным приоритетом (только root):
   ```bash
   sudo nice -n -20 bash -c "while true; do :; done" &
   ```
   Проверьте и объясните результат.

---

### Задание 5: Управление системными службами systemd

1. Просмотрите список всех служб:
   ```bash
   systemctl list-units --type=service --all --no-pager | head -20
   ```

2. Проверьте статус службы `sshd`:
   ```bash
   systemctl status sshd
   ```
   Запишите: активна ли она, PID, время запуска.

3. Остановите службу `sshd`:
   ```bash
   sudo systemctl stop sshd
   ```

4. Проверьте, что служба остановлена:
   ```bash
   systemctl is-active sshd
   ```

5. Запустите службу обратно:
   ```bash
   sudo systemctl start sshd
   ```

6. Проверьте, включена ли автозагрузка:
   ```bash
   systemctl is-enabled sshd
   ```

7. Отключите автозагрузку (временно):
   ```bash
   sudo systemctl disable sshd
   ```

8. Включите обратно:
   ```bash
   sudo systemctl enable sshd
   ```

---

### Задание 6: Анализ использования ресурсов

1. Проверьте использование памяти:
   ```bash
   free -h
   ```
   Запишите: total, used, free, shared, buff/cache, available.

2. Запустите несколько фоновых процессов для нагрузки на CPU:
   ```bash
   for i in {1..4}; do dd if=/dev/zero of=/dev/null bs=1M count=1000 & done
   ```

3. В отдельном терминале отслеживайте `top` и найдите процессы `dd`.

4. Завершите все `dd` процессы:
   ```bash
   pkill -f "dd if=/dev/zero"
   ```

5. Проверьте использование swap:
   ```bash
   swapon --show
   ```

6. Анализ процессов по использованию памяти:
   ```bash
   ps aux --sort=-%mem | head -10
   ```

---

### Задание 7: Практика systemd (журналы и управление)

1. Просмотрите журналы службы `sshd`:
   ```bash
   journalctl -u sshd --since "1 hour ago" | tail -20
   ```

2. Перезапустите службу и посмотрите актуальные логи:
   ```bash
   sudo systemctl restart sshd
   journalctl -u sshd -f
   ```
   (В отдельном терминале, нажмите Ctrl+C для выхода)

3. Попробуйте перезагрузить конфигурацию (если поддерживается):
   ```bash
   sudo systemctl reload sshd
   ```

4. Проверьте, какие службы запущены в данный момент:
   ```bash
   systemctl --type=service --state=running
   ```

5. Найдите службы, которые завершились с ошибкой:
   ```bash
   systemctl --type=service --state=failed
   ```

---

## Индивидуальные задания (25 вариантов)

> 🔑 **Важно:** Студенты выполняют только одно индивидуальное задание согласно своему варианту (1-25). Все операции выполняются в ALT Linux. Если какая-то служба отсутствует в системе, выполните задание для другой доступной службы из списка: `cron`, `rsyslog`, `sshd`, `dbus`, `polkit`, `getty@tty1`. Обязательно запишите в отчёте, какую службу использовали.

**Задача для каждого варианта:**

1. **Анализ процесса**: Найдите процесс из таблицы с помощью команды `ps aux`. Запишите: PID, PPID, USER, %CPU, %MEM, COMMAND.
2. **Проверка службы**: Выполните `systemctl status <служба>`. Запишите: активна ли, PID (если есть), время запуска.
3. **Управление службой**: 
   - Остановите службу: `sudo systemctl stop <служба>`
   - Проверьте, что процесс завершился (команда `ps -p <PID>` или `pgrep <имя_процесса>`)
   - Запустите службу обратно: `sudo systemctl start <служба>`
   - Проверьте, что служба активна: `systemctl is-active <служба>`
4. **Изменение приоритета (nice)**: 
   - Создайте фоновый процесс: `nice -n <начальное_значение> sleep 120 &`
   - Запишите PID и текущее nice: `ps -o pid,ni,cmd -p <PID>`
   - Измените приоритет: `renice -n <новое_значение> -p <PID>`
   - Проверьте изменение: `ps -o pid,ni,cmd -p <PID>`
5. **Просмотр логов**: Просмотрите последние 20 строк журнала службы: `journalctl -u <служба> -n 20`

> ⚠️ **Важно:** При остановке служб `sshd` не выполняйте задание через удалённое SSH-подключение — используйте локальную консоль виртуальной машины, иначе потеряете доступ. Службы `dbus` и `polkit` могут повлиять на работу графической среды; если используете GUI, будьте осторожны.

| Вариант | Процесс (имя в `ps`) | Служба (`systemctl`) | nice (начальное → конечное) | Журнал (service name) |
|---------|---------------------|----------------------|----------------------------|-----------------------|
| 1 | cron | cron.service | 3 → 8 | cron |
| 2 | rsyslogd | rsyslog.service | 5 → 12 | rsyslog |
| 3 | sshd | sshd.service | 2 → 7 | sshd |
| 4 | dbus-daemon | dbus.service | 8 → 3 | dbus |
| 5 | polkitd | polkit.service | 4 → 10 | polkit |
| 6 | getty | getty@tty1.service | 6 → 13 | getty |
| 7 | cron | cron.service | 1 → 9 | cron |
| 8 | rsyslogd | rsyslog.service | 7 → 14 | rsyslog |
| 9 | sshd | sshd.service | 5 → 11 | sshd |
| 10 | dbus-daemon | dbus.service | 6 → 13 | dbus |
| 11 | polkitd | polkit.service | 2 → 10 | polkit |
| 12 | getty | getty@tty1.service | 8 → 15 | getty |
| 13 | cron | cron.service | 4 → 12 | cron |
| 14 | rsyslogd | rsyslog.service | 3 → 11 | rsyslog |
| 15 | sshd | sshd.service | 9 → 16 | sshd |
| 16 | dbus-daemon | dbus.service | 5 → 13 | dbus |
| 17 | polkitd | polkit.service | 7 → 14 | polkit |
| 18 | getty | getty@tty1.service | 2 → 10 | getty |
| 19 | cron | cron.service | 6 → 14 | cron |
| 20 | rsyslogd | rsyslog.service | 1 → 9 | rsyslog |
| 21 | sshd | sshd.service | 4 → 12 | sshd |
| 22 | dbus-daemon | dbus.service | 9 → 16 | dbus |
| 23 | polkitd | polkit.service | 3 → 11 | polkit |
| 24 | getty | getty@tty1.service | 5 → 13 | getty |
| 25 | cron | cron.service | 7 → 15 | cron |

**Примечание:** 
- Для службы `getty@tty1` используйте полное имя при проверке статуса: `systemctl status getty@tty1`.
- Если процесс имеет другое имя (например, `crond` вместо `cron`), найдите его с помощью `pgrep -l <часть_имени>`.
- При работе с `systemctl` используйте `sudo` если у вас нет прав root.

---

**Инструкция для студента (application):**
1. Найдите процесс из таблицы (например, для варианта 1 — `cron`) с помощью `ps aux | grep cron`. Запишите PID и основные параметры.
2. Проверьте статус соответствующей службы через `systemctl status cron.service`.
3. Остановите службу `sudo systemctl stop cron.service`, убедитесь, что процесс завершился (повторите `ps`), затем запустите обратно `sudo systemctl start cron.service`.
4. Для демонстрации `nice`: запустите фоновый процесс `nice -n 3 sleep 120 &`, определите его PID, затем измените nice на 8 с помощью `renice -n 8 -p <PID>`.
5. Просмотрите логи службы через `journalctl -u cron -n 20`.

> ⚠️ **Внимание:** Некоторые процессы (особенно systemd, kernel) **нельзя** завершать — объясните почему в отчёте. При остановке sshd используйте локальную консоль, а не SSH.

---

## Дополнительные задания для всех вариантов

### Задание A: Создание скрипта для мониторинга
Напишите скрипт `monitor.sh`:
```bash
#!/bin/bash
echo "=== Мониторинг процессов и служб ==="
echo "Текущее время: $(date)"
echo
echo "Топ-5 процессов по использованию CPU:"
ps aux --sort=-%cpu | head -6
echo
echo "Топ-5 процессов по использованию памяти:"
ps aux --sort=-%mem | head -6
echo
echo "Службы в состоянии failed:"
systemctl --type=service --state=failed
echo
echo "Использование памяти:"
free -h
```

1. Сделайте скрипт исполняемым: `chmod +x monitor.sh`
2. Запустите: `./monitor.sh`
3. Добавьте вывод в отчёт.

### Задание B: Анализ zombie-процессов
1. Создайте простой zombie-процесс:
   ```bash
   ( while true; do ( (sleep 1;) & wait; ) done ) &
   ```
2. Найдите zombie-процессы:
   ```bash
   ps aux | grep '^.* Z .*$'
   ```
3. Завершите родительский процесс zombie.
4. Объясните, что такое zombie и как он образуется.

### Задание C: Управление службой через systemd
1. Создайте простой bash-скрипт `/usr/local/bin/hello.sh`:
   ```bash
   #!/bin/bash
   while true; do
     echo "Служба работает: $(date)" >> /tmp/hello.log
     sleep 30
   done
   ```
   Сделайте исполняемым: `chmod +x /usr/local/bin/hello.sh`.

2. Создайте unit-файл службы `/etc/systemd/system/hello.service`:
   ```ini
   [Unit]
   Description=Test Hello Service
   After=network.target

   [Service]
   Type=simple
   ExecStart=/usr/local/bin/hello.sh
   Restart=on-failure

   [Install]
   WantedBy=multi-user.target
   ```

3. Загрузите службу в systemd:
   ```bash
   sudo systemctl daemon-reload
   ```

4. Запустите и включите автозагрузку:
   ```bash
   sudo systemctl start hello
   sudo systemctl enable hello
   ```

5. Проверьте статус и логи:
   ```bash
   systemctl status hello
   journalctl -u hello -f
   ```

6. Остановите и удалите службу:
   ```bash
   sudo systemctl stop hello
   sudo systemctl disable hello
   sudo rm /etc/systemd/system/hello.service
   sudo systemctl daemon-reload
   ```

---

## Контрольные вопросы

1. Какая разница между `kill` и `kill -9`? Когда каждый следует использовать?
2. Что такое `nice` и `renice`? Какова шкала приоритетов?
3. Что такое zombie-процесс? Как его обнаружить и устранить?
4. Какие команды `ps` используются для:
   - Просмотра всех процессов?
   - Просмотра дерева процессов?
   - Поиска процесса по имени?
5. Что показывает `top`? Какие клавиши управления в нём есть?
6. В чём отличие `systemd` от старых систем инициализации (SysV init)?
7. Какая команда `systemctl` используется для перезагрузки конфигурации службы без её остановки?
8. Где хранятся логи systemd? Как просмотреть логи конкретной службы?
9. Что делает команда `systemctl enable`? Чем отличается от `systemctl start`?
10. Как проверить, активна ли служба? Какие возможные состояния у службы systemd?
11. Что такое PID 1? Почему его нельзя завершить?
12. Как найти все процессы, запущенные от имени определённого пользователя?
13. Что такое демон (daemon) и чем он отличается от обычного процесса?
14. Как определить, какая служба использует определённый порт (например, 80)?
15. Как приоритет процесса влияет на его выполнение?

---

## Требования к отчёту (минималистичный формат)

**Формат:** текстовый файл `отчет_лабораторная_5_<вариант>.txt` (без скриншотов).

**Содержание:**

1. **ФИО, группа, вариант** (1–25).

2. **Результаты индивидуального задания:**
   - Служба: `<имя_службы>`
   - Процесс: PID=<PID>, %CPU=<значение>, %MEM=<значение>
   - Статус службы до: `<active/inactive>`, после остановки: `<active/inactive>`, после запуска: `<active/inactive>`
   - nice: было <N1>, стало <N2> (привести вывод `ps -o pid,ni,cmd`)
   - Логи (последние 3 строки): вывести `journalctl -u <служба> -n 3`

3. **Дополнительные задания (указать, какие выполнены):**
   - A: вывести первые 10 строк результата `./monitor.sh`
   - B: объяснение zombie (2–3 предложения)
   - C: вывод `systemctl status hello` после создания службы

4. **Ответы на контрольные вопросы:** 1–2 предложения на каждый из 15 вопросов.

5. **Выводы (3–5 предложений):** Что нового узнали, какие команды полезны, сложности.

**Не нужно:** скриншоты, подробные описания шагов, многостраничные документы. Отчёт должен быть ёмким и проверяемым автоматически (через анализ текста).
---

## Возможные ошибки и способы устранения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `bash: kill: (PID) - Permission denied` | Нет прав root на завершение чужого процесса | Использовать `sudo kill` или войти как root. Некоторые системные процессы защищены от завершения обычными пользователями. |
| `bash: renice: Permission denied` | Попытка изменить nice без прав (уменьшить nice, т.е. повысить приоритет) | Обычный пользователь может только увеличивать nice (понижать приоритет). Для уменьшения нужен root. |
| `bash: systemctl: command not found` | systemd не установлен или не в PATH | ALT Linux использует systemd по умолчанию. Проверить: `which systemctl`. Если нет — установить `systemd-sysv`. |
| `Failed to stop <служба>.service: Unit <служба>.service not loaded.` | Служба не установлена или отключена | Проверить существование: `systemctl list-unit-files | grep служба`. Если службы нет в списке, возможно, она не установлена; выберите другую службу из списка доступных. |
| `Job for <служба>.service failed because the control process exited with error code.` | Ошибка в unit-файле или исполняемом файле | Проверить логи: `journalctl -u служба -xe`. |
| `No such file or directory` при создании unit-файла | Неправильный путь или нет прав | Убедиться в правильности пути `/etc/systemd/system/` и наличии прав `sudo`. |
| `Zombie processes appear in ps output` | Родительский процесс не вызвал `wait()` | Zombie не потребляет ресурсов (только запись в таблице процессов). Завершить родительский процесс или дождаться. |
| `ps: unknown option: p` | Неправильный синтаксис команды | Проверить синтаксис. В некоторых дистрибутивах `ps -p` работает, в других `ps --pid`. |
| `free показывает 0 swap` | Swap не настроен (нет раздела/файла) | Это нормально для некоторых конфигураций. Пропустить задание, если swap действительно отсутствует. |
| `bash: sudo: command not found` | Нет sudo в системе | Войти как root: `su -`, установить sudo: `apt-get install sudo`. |

---

## Дополнительные материалы

### Документация
```bash
man ps        # Подробное руководство по ps
man top
man kill
man nice
man renice
man systemctl
man systemd.unit
man systemd.service
```

### Ссылки для углублённого изучения
- **man7.org**: `man 7 signal` (сигналы), `man 5 proc` (procfs)
- **systemd**: `man systemd.special` (специальные юниты)
- **ALT Linux docs**: https://wiki.altlinux.org/

### Расширенные команды для экспериментов
```bash
# Мониторинг в реальном времени с фильтрацией
top -p $(pgrep -d',' nginx)

# Ищем процессы, у которых командная строка содержит "python"
ps -eo pid,user,cmd | grep python

# Завершение всех дочерних процессов
pkill -P <PID_родителя>

# Просмотр всех открытых файлов процесса
lsof -p <PID>

# Просмотр дерева/systemd
systemctl list-dependencies multi-user.target

# Принудительная перезагрузка службы (SIGHUP)
systemctl kill -s HUP <служба>
```

---

## Чек-лист для преподавателя (подготовка к занятию)

✅ **Перед началом лабораторной:**
- [ ] В базовом образе ALT Linux установлен `htop` (опционально, для удобства)
- [ ] Создан снапшот ВМ с чистым состоянием
- [ ] Проверено наличие прав sudo у студенческого пользователя (или доступ root)
- [ ] Готов индивидуальные варианты (1-25) и распределены среди студентов
- [ ] Подготовлен шаблон отчёта (структура из раздела "Требования к отчёту")
- [ ] Проверена доступность ключевых служб: `sshd`, `cron`, `rsyslog`, `dbus`, `polkit`, `getty@tty1`
- [ ] Установлены базовые утилиты: `ps`, `top`, `kill`, `systemctl` (они есть по умолчанию)

✅ **Во время занятия:**
- [ ] Объяснить разницу Between процессами и службами
- [ ] Показать примеры корректного завершения процессов
- [ ] Предупредить: не завершать системные процессы (PID < 100, kernel threads)
- [ ] Проверить, что студенты понимают вывод `ps aux`
- [ ] Убедиться, что студенты используют локальную консоль при работе с sshd

✅ **После завершения:**
- [ ] Проверить, что студенты восстановили все службы (если их останавливали)
- [ ] Убедиться, что ВМ в рабочем состоянии
- [ ] Обновить снапшот ВМ для следующей лабораторной

---

## Полезные команды (шпаргалка)

```bash
# Процессы
ps aux                              # все процессы
ps -ef                              # альтернативный формат
ps -p PID -o pid,user,cmd          # конкретный процесс
pstree -p                          # дерево процессов с PID
pgrep -l имя_процесса              # поиск PID по имени
pidof имя_процесса                  # получить PID

# Завершение
kill PID                            # SIGTERM
kill -9 PID                         # SIGKILL
pkill имя_процесса                  # по имени
killall имя_процесса                # все процессы с именем

# Приоритеты
nice -n 10 команда &                # запуск с nice 10
renice -n 5 -p PID                  # изменить nice
nice                                # показать текущий nice

# systemd
systemctl status служба             # статус
systemctl start/stop служба         # запуск/остановка
systemctl restart служба            # перезапуск
systemctl enable/disable служба     # автозагрузка
systemctl is-active служба          # активна ли?
systemctl is-enabled служба         # включена ли?
journalctl -u служба                # логи службы
journalctl -u служба -f             # логи в реальном времени
systemctl list-units --type=service # все службы
systemctl --type=service --state=failed # failed службы

# Ресурсы
top                                # мониторинг
htop                               # улучшенный мониторинг
free -h                            # память
df -h                              # диски
```

---

## Примечания по специфике ALT Linux

1. В ALT Linux используется **systemd** как система инициализации по умолчанию (начиная с версии 7 и выше).
2. Для управления пакетами используется `apt-get` (гибридный `apt-rpm`), но это не относится к процессам/службам.
3. Команды `ps`, `kill`, `systemctl` — стандартные GNU/Linux, идентичны другим дистрибутивам.
4. Если `systemctl` недоступен, возможно используется старая система инициализации (SysV). Но в ALT Linux p11 systemd стандартен.
5. Для просмотра журналов вместо `tail -f /var/log/syslog` используйте `journalctl`.
6. Для работы с `journalctl` может потребоваться вхождение в группу `systemd-journal` или использование `sudo`.

---

Выполнив все задания, студент должен понимать жизненный цикл процессов, уметь управлять ими и системными службами, анализировать использование ресурсов, а также работать с systemd journal.
